# Copier vers `.env` en local (fichier ignoré par Git — ne jamais committer).
DATABASE_URL="postgresql://user:password@localhost:5432/juriscan?schema=public"
# OCR réel Gemini (Google AI Studio) — sans cette clé, repli simulation locale.
GOOGLE_GENERATIVE_AI_API_KEY=""
